/* htmx is loaded here at build time — fetch during Docker build.
 * This placeholder lets the templates ship without a bundler; for production,
 * replace this file with the official htmx build (https://unpkg.com/htmx.org).
 */
